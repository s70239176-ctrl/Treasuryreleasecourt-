{
  "name": "treasury-release-court",
  "private": true,
  "version": "1.0.0",
  "description": "A DAO tranche sits in an Intelligent Contract until an AI jury decides work is DONE against a written charter.",
  "scripts": {
    "deploy": "tsx deploy/deploy.ts",
    "test:direct": "gltest tests/direct",
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "genlayer-js": "^0.8.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "dotenv": "^16.4.5"
  }
}
